input {
  kafka {
    bootstrap_servers => ["kafka:9092"]
    topics => ["rsyslog"]
    auto_offset_reset => "earliest"
  }
}


output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "rsyslog"
  }
}
